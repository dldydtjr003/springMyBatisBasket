<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zeus.mapper.UserMapper">

    <insert id="create">
        INSERT INTO USERS (no, id, password, name, email, phone)
        VALUES (USERS_SEQ.NEXTVAL, #{id}, #{password}, #{name}, #{email}, #{phone})
    </insert>

    <select id="list" resultType="com.zeus.domain.Users">
        SELECT no, id, name, email, phone 
        FROM USERS
        ORDER BY no DESC
    </select>

    <select id="read" resultType="com.zeus.domain.Users">
        SELECT * FROM USERS WHERE no = #{no}
    </select>

    <update id="update">
        UPDATE USERS 
        SET password = #{password}, name = #{name}, email = #{email}, phone = #{phone}
        WHERE no = #{no}
    </update>

    <delete id="delete">
        DELETE FROM USERS WHERE no = #{no}
    </delete>

</mapper>